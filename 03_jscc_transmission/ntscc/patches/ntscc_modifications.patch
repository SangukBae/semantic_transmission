diff --git a/config.py b/config.py
index ae03725..9882987 100644
--- a/config.py
+++ b/config.py
@@ -5,8 +5,8 @@ import torch.nn as nn
 
 class config:
 
-    train_data_dir = ['path to DIV2K_train_HR or OpenImages']
-    test_data_dir = ['Path to kodak']
+    train_data_dir = ['/path/to/OpenImages_train']
+    test_data_dir = ['/path/to/test_images']
     batch_size = 10
     num_workers = 8
 
@@ -21,10 +21,10 @@ class config:
     distortion_metric = 'MSE'  # 'MS-SSIM'
 
     use_side_info = False
-    train_lambda = 64
-    eta = 0.2
+    train_lambda = 64 #越大cbr越小
+    eta = 0.2 #0.2 #越大cbr越大
 
-    channel = {"type": 'awgn', 'chan_param': 10}
+    channel = {"type": 'awgn', 'chan_param': 10} #这个chan_param就是snr
     multiple_rate = [16, 32, 48, 64, 80, 96, 102, 118, 134, 160, 186, 192, 208, 224, 240, 256]
     ga_kwargs = dict(
         img_size=(image_dims[1], image_dims[2]),
diff --git a/main.py b/main.py
index 1fefd3a..ab991ed 100644
--- a/main.py
+++ b/main.py
@@ -164,7 +164,7 @@ def parse_args(argv):
         '--save_log', action='store_true', default=True, help='Save log to disk'
     )
     parser.add_argument("--checkpoint",
-                        default="checkpoints/PSNR_SNR=10_gaussian/ntscc_hyperprior_quality_4_psnr.pth",
+                        default="checkpoints/ntscc_hyperprior_quality_4_psnr.pth",
                         type=str, help="Path to a checkpoint")
     args = parser.parse_args(argv)
     return args
diff --git a/net/NTSCC_Hyperior.py b/net/NTSCC_Hyperior.py
index f11045d..13dd2ec 100644
--- a/net/NTSCC_Hyperior.py
+++ b/net/NTSCC_Hyperior.py
@@ -4,7 +4,8 @@ import math
 import torch.nn as nn
 from loss.distortion import Distortion
 from compressai.entropy_models import EntropyBottleneck, GaussianConditional
-from compressai.ops import ste_round
+# from compressai.ops import ste_round
+from compressai.ops import quantize_ste
 from layer.layers import Mlp
 from layer.analysis_transform import AnalysisTransform
 from layer.synthesis_transform import SynthesisTransform
@@ -56,12 +57,12 @@ class NTC_Hyperprior(nn.Module):
         _, z_likelihoods = self.entropy_bottleneck(z)
         z_offset = self.entropy_bottleneck._get_medians()
         z_tmp = z - z_offset
-        z_hat = ste_round(z_tmp) + z_offset
+        z_hat = quantize_ste(z_tmp) + z_offset
 
         gaussian_params = self.hs(z_hat)
         scales_hat, means_hat = gaussian_params.chunk(2, 1)
         _, y_likelihoods = self.gaussian_conditional(y, scales_hat, means=means_hat)
-        y_hat = ste_round(y - means_hat) + means_hat
+        y_hat = quantize_ste(y - means_hat) + means_hat
         x_hat = self.gs(y_hat)
         mse_loss = self.distortion(input_image, x_hat)
         bpp_y = torch.log(y_likelihoods).sum() / (-math.log(2) * H * W) / B
@@ -124,7 +125,7 @@ class NTSCC_Hyperprior(NTC_Hyperprior):
         s_masked, mask_BCHW, indexes = self.fe(y, y_likelihoods.detach(), eta=self.eta)
 
         # Pass through the channel.
-        mask_BCHW = mask_BCHW.byte()
+        mask_BCHW = mask_BCHW.bool()
         channel_input = torch.masked_select(s_masked, mask_BCHW)
         channel_output, channel_usage = self.channel.forward(channel_input)
         s_hat = torch.zeros_like(s_masked)
diff --git a/requirements.txt b/requirements.txt
index b5b103e..110fc60 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,5 +1,6 @@
 # This file may be used to create an environment using:
 # $ conda create --name <env> --file <this file>
 # platform: linux-64
-compressai==1.2.0
-timm==0.5.4
+compressai==1.2.6
+timm==0.9.16
+numpy==1.24.3
